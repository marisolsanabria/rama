package com.example.rama.domain.data.network

import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import com.google.firebase.auth.FirebaseAuth
import com.google.firebase.database.FirebaseDatabase
import com.google.firebase.firestore.FirebaseFirestore
import com.google.firebase.firestore.QueryDocumentSnapshot

class Repo {
    /*
    fun getProcessOfTheFileData():LiveData<MutableList<com.example.rama.utils.Process>>{
        val mutableData= MutableLiveData<MutableList<Process>>()
        val currentUser=FirebaseAuth.getInstance().currentUser!!.uid



        FirebaseDatabase.getInstance().reference.child(currentUser).child("process").get().addOnSuccessListener { result ->
            for (document:QueryDocumentSnapshot in result){ // este problema es porque lo hag con FirebaseDatabase

            }
        }

        //acá haré la query que necesitaré para traer los dato desde firebase

    }

     */
}


